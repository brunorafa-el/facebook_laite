<% if current_user.like?(post) %>
  <%= button_to "Dislike", dislike_path(post_id: post.id), remote: true, method: :delete %>
<% else %>
  <%= button_to "Like", like_path(post_id: post.id), remote: true %>
<% end %>
